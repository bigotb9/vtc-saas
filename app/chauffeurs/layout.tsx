import type { Metadata } from "next"
export const metadata: Metadata = { title: "Chauffeurs — Boyah Group" }
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</> }
